{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Build Active C++ File",
            "command": "g++ ${file} -std=c++17 -pthread -o ${fileBasenameNoExtension}",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}